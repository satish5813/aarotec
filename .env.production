# Production environment — committed on purpose (contains no secrets).
# Used by `next build` so canonical / Open Graph / sitemap / robots URLs
# always resolve to the live domain instead of localhost.
NEXT_PUBLIC_SITE_URL=https://www.aarotec.in
